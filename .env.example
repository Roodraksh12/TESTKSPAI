# Env files live next to each app (do not use a root .env):
#
#   backend/.env   ← FastAPI secrets + Supabase DB
#   frontend/.env  ← Vite public keys only
#
# See backend/.env.example and frontend/.env.example for the full key lists.
