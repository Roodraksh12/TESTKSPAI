# Frontend Vite — copy to frontend/.env and fill values
# No maps API key needed — the hotspot map uses Leaflet + OpenStreetMap/CARTO tiles.

VITE_API_BASE_URL=http://localhost:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
